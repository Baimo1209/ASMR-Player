# ASMR Player v1.3.0

## 项目介绍

ASMR Player 是一款安卓本地 ASMR 音声播放器，用于整理和播放手机文件夹中的音频、图片与 WebVTT 同步台词。应用适配常见音声作品目录结构，例如 `RJ01534688/1-普通版/MP3`，会递归识别作品、音轨、图片和台词文件，并提供作品列表、音轨列表、播放页和台词页。

应用安装后的显示名称为“白沫播放器”。

## 本版本亮点

- 新增上深下浅的主页视觉结构，下方内容面板使用顶部圆角。
- 作品、音轨、台词和最近播放列表改为轻量卡片样式。
- 底部导航保留“听过 / 我的 / 设置”，移除不稳定的内置网页“寻找”页。
- 迷你播放器改为悬浮胶囊，位于底部导航上方。
- 支持沉浸式隐藏系统状态栏和底部导航栏，减少遮挡。
- “听过”记录近期播放音频，点击可跳转并播放。
- 作品列表滚动性能优化：列表行复用、封面缩略图内存缓存、后台解码图片。
- 设置页保留选择文件夹和台词悬浮功能。

## 已有功能

- 支持 `.mp3` 和 `.wav` 音频播放。
- 使用 Android 系统文件夹选择器读取目录。
- 递归扫描并自动按作品分组。
- 支持 `.jpg`、`.jpeg`、`.png`、`.webp`、`.bmp` 图片。
- 支持 `.vtt` 同步台词，包括 `track.vtt` 和 `track.mp3.vtt`。
- 台词页可上下滑动查看完整台词，点击段落可跳转播放位置。
- 返回作品页或音轨页后保持播放。
- 底部迷你播放条支持暂停、上一首、下一首，并可快速回到播放页。

## 安装

下载本 Release 中的 `ASMR-Player-v1.3.0-debug.apk`，在安卓设备上允许安装未知来源应用后安装。

## 说明

当前发布包为 debug 签名 APK，适合个人测试和侧载安装。
