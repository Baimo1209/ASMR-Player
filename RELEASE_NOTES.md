# 白沫播放器 v1.0.0

## 软件介绍

白沫播放器是一款面向本地 ASMR 音声文件夹的安卓播放器。它会从系统文件夹选择器读取用户选中的目录，递归识别类似 `RJ01534688/1-普通版/MP3` 的作品结构，并自动整理为作品列表、音轨列表和播放页面。

## 主要功能

- 支持 `.mp3` 和 `.wav` 音频播放。
- 自动按作品分组，主页面显示作品封面、作品名和音轨数量。
- 支持同目录图片作为作品封面或音轨图片。
- 支持 `.vtt` 同步台词，包括 `track.vtt` 和 `track.mp3.vtt` 命名。
- 播放页显示图片、同步台词、播放进度和控制按钮。
- 台词页可上下滑动查看完整台词，点击任意台词段落可跳转播放到对应时间。
- 返回作品页或音轨页时保持播放，底部迷你播放条可暂停、上一首、下一首，并可回到播放页。

## 安装

下载 Release 中的 `白沫播放器-debug.apk`，在安卓设备上允许安装未知来源应用后安装。

## 说明

当前发布包为 debug 签名 APK，适合个人测试和侧载安装。
