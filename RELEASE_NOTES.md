# ASMR Player v1.1.0

## 项目介绍

ASMR Player 是一款安卓本地 ASMR 音声播放器，用于整理和播放手机文件夹中的音频、图片与 WebVTT 同步台词。应用适配常见音声作品目录结构，例如 `RJ01534688/1-普通版/MP3`，会递归识别作品、音轨、图片和台词文件，并提供作品列表、音轨列表、播放页和台词页。

应用安装后的显示名称为“白沫播放器”。

## 本版本亮点

- 作品列表作为主页面，可上下滑动浏览作品。
- 支持长按作品并点击目标位置调整作品顺序。
- 支持长按音轨并点击目标位置调整音轨顺序。
- 播放页图片完整显示，避免封面被裁切。
- 同一音轨存在多张图片时，可点击播放页图片轮播切换。
- 播放/暂停和台词入口改为图标按钮，界面更简洁。
- 台词显示会保持到下一段台词开始，不再在空档期清空。

## 已有功能

- 支持 `.mp3` 和 `.wav` 音频播放。
- 使用 Android 系统文件夹选择器读取目录。
- 递归扫描并自动按作品分组。
- 支持 `.jpg`、`.jpeg`、`.png`、`.webp`、`.bmp` 图片。
- 支持 `.vtt` 同步台词，包括 `track.vtt` 和 `track.mp3.vtt`。
- 台词页可上下滑动查看完整台词，点击段落可跳转播放位置。
- 返回作品页或音轨页后保持播放。
- 底部迷你播放条支持暂停、上一首、下一首，并可快速回到播放页。

## 安装

下载本 Release 中的 `ASMR-Player-v1.1.0-debug.apk`，在安卓设备上允许安装未知来源应用后安装。

## 说明

当前发布包为 debug 签名 APK，适合个人测试和侧载安装。
